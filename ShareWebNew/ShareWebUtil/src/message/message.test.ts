import { expect } from 'chai';
import * as sinon from 'sinon';
import { listen } from './message';


describe('ShareWebUtil', () => {
    describe('message', () => {

        describe('注册消息处理', () => {
            it('传入消息名，和消息处理函数：正确注册消息处理');
        });

    });
}); 