import { expect } from 'chai';
import { copy } from './clipboard';

declare const { describe, it }

describe('ShareWebUtil', () => {
    describe('clipboard', () => {
        it('剪贴板写入#copy');
    });
})