import i18n from '../../i18n';
export default i18n([

    [
        '您确定要将密级为 “${csflevel}” 的文件 “${name}” 共享给指定的访问者吗？',
        '您確定要將密級為 “${csflevel}” 的檔案 “${name}” 共用給指定的訪客嗎？',
        'Are you sure to share the file "${name}" of "${csflevel}" security level to specified visitors?'
    ]
])