import i18n from '../i18n';

export default i18n([
    [
        '最多可添加${maxTags}个标签。',
        '最多可新增${maxTags}個標籤。',
        'At most ${maxTags} tags can be added.'
    ], [
        '标签名不能包含 # \\ / : * ? " < > | 特殊字符。',
        '標籤名不能包含 # \\ / : * ? " < > | 特殊字元。',
        'Tag name cannot contain # \\ / : * ? " < > | special characters.'
    ]
])
