declare namespace UI {
    namespace NWProvider {
        interface Props extends React.Props<any> {
            /**
             * DOM Window
             */
            window: Window;
        }
    }
}