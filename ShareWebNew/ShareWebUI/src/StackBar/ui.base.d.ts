declare namespace UI {
    namespace StackBar {
        type Props = {
            width: string,
            className?: string
        }
        interface Base {
            props: Props
        }
    }
}